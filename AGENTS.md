# AGENTS.md

## Repo rules

- 답변은 한국어로 한다.
- 코드 생성, 코드 수정, 리팩터링 뒤에는 반드시 `npm run lint:fix` 를 먼저 실행한다.
- `npm run lint:fix` 뒤에는 반드시 `npm run lint` 를 실행해 통과 여부를 확인한다.
- 가능하면 완료 보고 전에 `npm run build` 로도 확인한다.
- 자동 훅이 없으므로 이 파일 지침을 우선 따른다.
